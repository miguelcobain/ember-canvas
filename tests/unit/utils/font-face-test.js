import fontFace from 'dummy/utils/font-face';
import { module, test } from 'qunit';

module('Unit | Utility | font face');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fontFace();
  assert.ok(result);
});
