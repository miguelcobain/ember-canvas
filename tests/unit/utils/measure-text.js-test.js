import measureTextJs from 'dummy/utils/measure-text.js';
import { module, test } from 'qunit';

module('Unit | Utility | measure text.js');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = measureTextJs();
  assert.ok(result);
});
