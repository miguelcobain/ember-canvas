import frame from 'dummy/utils/frame';
import { module, test } from 'qunit';

module('Unit | Utility | frame');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = frame();
  assert.ok(result);
});
